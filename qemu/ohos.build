{
    "subsystem": "device_qemu",
    "parts": {
        "device_qemu": {
            "module_list": [
                "//device/board/${product_company}/${device_name}:qemu_group"
            ],
            "test_list": [
                "//device/board/${product_company}/${device_name}:rootfs_pkg"
            ]
        }
    }
}
